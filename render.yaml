# render.yaml
services:
  - type: web
    name: auto-inspect-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    pythonVersion: "3.11"
    envVars:
      - key: BOT_TOKEN
        sync: false
